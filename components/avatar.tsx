import { scale } from "@/deps/scale";
import { View } from "react-native";


export default function Avatar () {
    return (
        <View style={{width: scale(44), height: scale(44), borderRadius: 44, backgroundColor: '#8e8e93'}}></View>
    )
}
